# OS cruft
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store

# Editors
.vscode/
.idea/

# Claude Code local files (settings, launch config)
.claude/

# Local machine paths for the photo-intake tooling
tools/config.json

# Unprocessed phone originals never belong in the repo — the iCloud inbox
# stays outside it, and only converted/renamed files go in /media.
*.heic
*.HEIC
*.mov
*.MOV

# Scratch
*.tmp
~$*

# Downloaded intake manifests (transient)
zx6r-intake*.json
